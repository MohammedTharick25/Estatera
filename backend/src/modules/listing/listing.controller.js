const service = require("./listing.service");
exports.create = async (req, res) => { try { res.status(201).json(await service.create(req.body, req.files, req.app)); } catch (err) { console.error(err); res.status(500).json({ error: err.message }); } };
exports.findAll = async (req, res) => { try { res.json(await service.findAll(req.query)); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.findInventory = async (req, res) => { try { res.json(await service.findInventory(req.query)); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.findById = async (req, res) => { try { const listing = await service.findById(req.params.id); if (!listing) return res.status(404).json({ message: "Property not found" }); res.json(listing); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.updateStatus = async (req, res) => { try { res.json(await service.updateStatus(req.params.id, req.body.status)); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.setLifecycle = async (req, res) => { try { const listing = await service.setLifecycle(req.params.id, req.body.lifecycle, req.app); if (!listing) return res.status(404).json({ error: "Property not found" }); res.json(listing); } catch (err) { res.status(400).json({ error: err.message }); } };
exports.update = async (req, res) => { try { const listing = await service.update(req.params.id, req.body, req.files); if (!listing) return res.status(404).json({ message: "Property not found" }); res.json(listing); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.incrementViews = async (req, res) => { try { await service.incrementViews(req.params.id); res.status(200).json({ message: "View incremented" }); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.toggleLike = async (req, res) => { try { const result = await service.toggleLike(req.params.id, req.body.userId); if (!result) return res.status(404).json({ message: "Listing not found" }); res.json(result); } catch (err) { console.error(err); res.status(500).json({ message: "Server error" }); } };
exports.remove = async (req, res) => { try { await service.remove(req.params.id); res.json({ message: "Property deleted successfully" }); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.setArchived = async (req, res) => { try { const listing = await service.setArchived(req.params.id, req.body.archived !== false); if (!listing) return res.status(404).json({ error: "Property not found" }); res.json(listing); } catch (err) { res.status(500).json({ error: err.message }); } };
