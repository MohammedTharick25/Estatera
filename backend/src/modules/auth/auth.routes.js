const router = require("express").Router(); const controller = require("./auth.controller"); const { requireAuth } = require("../../middleware/auth");
router.post("/signup", controller.signup); router.post("/login", controller.login); router.post("/forgot-password", controller.requestPasswordReset); router.post("/verify-reset-otp", controller.verifyPasswordResetOtp); router.post("/reset-password", controller.resetPassword); router.post("/change-password", requireAuth, controller.changePassword); router.get("/sessions", requireAuth, controller.listSessions); router.delete("/sessions/:sessionId", requireAuth, controller.revokeSession); router.post("/sessions/revoke-others", requireAuth, controller.revokeOtherSessions); module.exports = router;
