const service = require("./user.service");
exports.updateProfile = async (req, res) => { try { const user = await service.updateProfile(req.body, req.file); if (user.error) return res.status(user.status).json({ error: user.error }); res.status(200).json({ user: { id: user._id, name: user.name, email: user.email, role: user.role, image: user.image, favorites: user.favorites } }); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.toggleFavorite = async (req, res) => { try { const favorites = await service.toggleFavorite(req.body); if (!favorites) return res.status(404).json({ error: "User not found" }); res.status(200).json({ favorites }); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.getFavorites = async (req, res) => { try { const favorites = await service.getFavorites(req.params.userId); if (!favorites) return res.status(404).json({ error: "User not found" }); res.json(favorites); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.getAll = async (_req, res) => { try { res.json(await service.getAll()); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.getStatus = async (req, res) => { try { const status = await service.getStatus(req.params.id); if (!status) return res.status(404).json({ error: "User not found" }); res.json(status); } catch (_) { res.status(500).json({ error: "Server error" }); } };
exports.toggleBlock = async (req, res) => { try { const result = await service.toggleBlock(req.params.id); if (result.error) return res.status(result.status).json({ error: result.error }); res.json(result); } catch (err) { res.status(500).json({ error: err.message }); } };
exports.remove = async (req, res) => { try { const result = await service.remove(req.params.id); if (result.error) return res.status(result.status).json({ error: result.error }); res.json(result); } catch (err) { res.status(500).json({ error: err.message }); } };
